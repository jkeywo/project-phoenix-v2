# Fleet-pinned toolchain. rustup reads this at cargo time, so CI and every
# contributor build with the same compiler; vellum's fleet-ci workflow defaults
# to the same channel for repos that lack the file.
[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"
