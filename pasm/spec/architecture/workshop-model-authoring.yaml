entities:
  - component: workshop-model-source-authoring
    core:
      status: implemented
      confidence: confirmed
      origin: ai
      title: Workshop Model Source Authoring
      summary: "The shared offline Authoring surface groups existing rig fields through the Workshop runtime inspector and provides exact-source structural authoring for markers, target points, model variants and LOD rungs. Every accepted operation enters the same draft and chronological history."
      rationale:
        - "[ai] Browser and native surfaces use their existing source providers. The panel has no project filesystem API, Live action route, model serializer or separate undo stack. Model and variant selection are local presentation."
        - "[ai] A grouped edit prepares each changed scalar through the source-span patch API. It writes one draft edit only after every patch succeeds and exact source, selected variant, draft identity and context remain current. A refusal or late response changes no source."
        - "[ai] Variant creation refuses overwriting an existing path and copies comments, line endings and unknown fields byte-for-byte. A model without a sidecar starts with the runtime's empty identity rig. Normal runtime validation remains the gate for Test, save and export."
        - "[ai] A variant is a named sidecar, not an ordered runtime list. Create, rename and remove are exact member transactions; the selector uses the established stable lexical order rather than persisting presentation-only ordering as model semantics."
        - "[ai] Structural marker, target-point and LOD operations replace, insert, remove or exchange only their owned source spans. Untouched comments, unknown keys, ordering and mixed line endings stay byte-identical; each operation is one WorkshopDocument history entry, so undo, recovery, save and export observe the same edit."
        - "[ai] Structure rows name their source file and line. Candidate rigs are parsed against runtime field shapes, checked for unit marker directions, ordered LOD bounds, one renderer per rung and captured asset/variant ownership, then passed through the ordinary runtime validator before the draft changes. Late or stale results are refused."
        - "[ai] The structural lists are labelled native controls with textual vectors and source provenance beside the captured picture. Their move boundaries are disabled, so keyboard use does not depend on drag or colour. Variant selection feeds the real captured renderer; its Auto/fixed LOD, marker gizmos and measured statistics remain the visual inspection path."
        - "[ai] Test holds these controls and hides the Authoring panel. Controls retain labels, keyboard access, visible non-colour status and wrapping layouts. External generation and structural metadata parity remain separate work."
        - "[ai] Model preview uses the explicit modelPreview provider and the shared disposable preview owner. Refresh captures source deliberately; edits and selection changes mark the retained picture stale. Test, draft replacement and exit retire the preview. Camera, distance, gizmos, lighting and LOD controls affect only this renderer, with statistics measured by ViewerPlugin."
        - "[ai] Issue #1470 supplies the browser producer behind that provider. workshop-preview.html is a private Trunk target built with no default features plus viewer, so the preview renders through the retained shared ViewerPlugin rather than a Workshop-only approximation; adding the viewer feature to the server bundle was rejected because it would ship preview code to every player for a surface only an author opens. The page declares no asset copy and no proxy, and register_snapshot binds its App to exactly the captured bytes before AssetPlugin, so an uncaptured project file is absent rather than merely slower to reach."
        - "[ai] The preview reuses the disposable Test's iframe, private MessagePort, transferable-snapshot guards and dependency merger, and captures a draft with the identical path/byte rule. A second capture path would be a second definition of captured content, which is precisely how an uncaptured file gets read. Its child accepts only local renderer commands over its own connect type, so a Test port cannot drive a preview or the reverse."
        - "[ai] The native producer materialises the same validated draft and immutable dependencies inside the source worker, then publishes each member at a nonce-scoped loopback-only immutable route. Its JSON bridge carries only the compact authored references, selection, revision, logical path list and route capability; it never serialises the merged asset bytes. A retired or unknown capture prefix returns Not Found before static bundle resolution, so a missing member cannot fall through to project or delivery files."
        - "[ai] One native capture generation owns those routes. Refresh replaces it only after validation; Test entry, draft replacement, explicit Stop, pane epoch failure/replacement and host exit withdraw it. Capture ids make a late release harmless. The embedded page fetches the frozen members and then uses the same preview child, ViewerPlugin, controls and measured statistics as browser Workshop."
    architecture:
      kind: authoring-presentation
      authority: non-authoritative
      coordinates: [gm-milestone-integrated-workshop, workshop-runtime-asset-validation]
      runs_in: [host-browser-runtime, native-host-runtime]
    implementation:
      status: observed
      paths: [editor/workshop-models.js, editor/workshop-model-structure.js, gui/workshop-models-panel.js, gui/workshop-model-preview-panel.js, gui/workshop-authoring.js, gui/workshop.css, scripts/build-workshop.mjs, editor/workshop-preview.js, editor/workshop-preview-runtime.js, gui/workshop-preview-boot.js, src/viewer/preview.rs, src/workshop/captured_source.rs, src/workshop/provider/preview_snapshot.rs, src/native_host/workshop/preview.rs, src/native_host/workshop/bridge.rs, src/delivery/serve.rs, scripts/copy-workshop-preview.mjs]
      symbols: [modelDocuments, createModelVariant, patchModelFields, inspectModelStructure, prepareModelStructureOperation, applyModelStructureOperation, mountWorkshopModels, mountWorkshopModelPreview, createBrowserWorkshopPreview, createWorkshopPreviewFrame, launchWorkshopPreview, viewer_workshop_preview_init, NativeWorkshopProvider::prepare_preview, PreviewRoutes]
      tests: [editor/tests/workshop-models.test.js, editor/tests/workshop-model-structure.test.js, tests/client/workshop-models-panel.test.js, tests/client/workshop-model-preview-panel.test.js, tests/client/workshop-authoring.test.js, tests/client/workshop-preview.test.js, tests/smoke/workshop-preview.render.spec.js, tests/native_workshop_ultralight.rs]
