[project]
name = "project-phoenix-pasm"
version = "0.1.0"
description = "Phase 0-6 PASM runtime for Project Phoenix."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0",
]

[project.scripts]
pasm = "pasm.cli.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = [
  "pasm",
  "pasm.architecture",
  "pasm.cli",
  "pasm.core",
  "pasm.domains",
  "pasm.domains.game_design",
  "pasm.implementation",
  "pasm.integration",
  "pasm.migration",
  "pasm.scanners",
]

[dependency-groups]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests/pasm"]
