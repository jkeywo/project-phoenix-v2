
Network messaging needs breaking up into type (i.e. general ship, nearby entities, console specific)
- Each console determines which messages in needs
- A client's consoles are combined to find which messages it needs.
