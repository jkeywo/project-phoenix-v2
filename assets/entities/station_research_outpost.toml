name = "entity.station_research_outpost.name"
tags = ["station", "comms_contact", "science_facility"]

[target]
tags = ["station"]
threat_level = "none"
description = "entity.station_research_outpost.target.description"

[hull]
hull_integrity = 60.0

[radar_appearance]
colour = [0.3, 0.8, 0.6]
size = 8.0
icon = "station"

[mesh]
model = "assets/models/alliance_research_outpost.glb"
# Same hull as `depot_transfer` and `station_outpost`, so the same stand-in:
# 7.56 x 3.37 x 7.59 measured, minor = 1.68 and major = 2.12 derived from it.
# See `depot_transfer.toml` for the arithmetic.
#
# This template carries no `[collider]`, so it was not part of the collider
# correction that fixed the other four — but its fallback drew a 20-unit ring
# for the same 7.6-unit hull, which is the same staleness with nothing to
# collide with. Left alone it would have been the one station still popping size
# when its GLB arrived.
shape = "torus"
radius = 2.12
minor_radius = 1.68
colour = [0.4, 0.7, 0.9]

[comms]
range = 600.0
