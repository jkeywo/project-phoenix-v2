entities:
  - component: red-alert-console-interface
    core:
      status: implemented
      confidence: confirmed
      title: Red Alert Console Interface
      summary: Captain-facing control that displays the ship alert state and requests an explicit target state.
      rationale:
        - A state-setting control is idempotent, unlike a toggle command that can be applied twice or arrive after the displayed state changes.
        - The control remains read-only while the red-alert system is AI-controlled.
        - "[ai] Issue #1398: RED ALERT IS THE CAPTAIN'S ONLY FIRING-POSTURE LEVER. The Weapons Hold button that sat beside it from issue #1041 is gone, with its semantic action, its KeyH binding, its set_weapons_hold command and its blackboard replica. Restraint is expressed by POWER now, so the control that expresses it is Engineering's — see weapons-cold-fire-gate and scripted-weapons-restraint-orders — and a captain reading this panel reads one posture rather than two that had to be composed in their head."
    architecture:
      kind: client-interface
      authority: non-authoritative
      reads:
        - red-alert-status-replica
      sends:
        - set-red-alert-command
      runs_in:
        - phone-browser-runtime
    implementation:
      status: declared
      paths: [gui/components/ph-red-alert.js, gui/action-map.js, gui/console-state.js]
      tests: [npx vitest run]

  - state: authoritative-red-alert-state
    core:
      status: implemented
      confidence: confirmed
      title: Authoritative Red Alert State
      summary: Per-ship host-owned boolean alert state, now written with explicit set semantics rather than toggle semantics.
    architecture:
      classification: authoritative
      owner: host-red-alert-control-router
      writers:
        - host-red-alert-control-router
      replicas:
        - red-alert-status-replica
        - selected-target-alert-state
    implementation:
      status: declared
      paths: [src/ship/state.rs, src/console/captain/server.rs]
      symbols: [ShipRedAlert]

  - component: weapons-cold-fire-gate
    core:
      status: implemented
      confidence: confirmed
      origin: ai
      title: Weapons Cold Fire Gate
      summary: "The gate that makes a switched-off power group mean a silent gun (issue #1396). Each firing decision reads the power group its own [[system]] entry authors — ShipConfig::power_group_for, resolved against the ship's reactor by PowerSystem::is_group_cold through the shared system_power_group_is_cold helper — and folds the answer into the WeaponsAlertPosture the bank's or tube's authored fire predicate already resolves, at WEAPONS_COLD_ALERT_FACT, the rung below stood-down. The same reading refuses a HUMAN order at the applier (handle_fire_phaser, handle_fire_blaster, handle_fire_torpedo) with WeaponActionRefusal::PowerCold. The ship-level weapons doctrine (tick_weapons_arc_request) reads the whole gun line instead of one mount and stops asking Helm to swing a family into line only when every emitter the hull carries is on a group that is off."
      rationale:
        - "[ai] The reading is PER SYSTEM, not per ship, because [[system]].power_group is authored per system. A hull is free to put its tubes on a different group than its beams; the gate then closes on the half that is switched off and leaves the other half shooting. Every shipped hull puts all of it on weapons today, so a ship-wide shortcut would have passed every test and quietly contradicted the authoring granularity the TOML already has."
        - "[ai] The gate is applied at BOTH origins. The authored-predicate half stops the AI asking; the applier half refuses a human's admitted order. A gate that lived only in the AI decider would constrain NPC crews and leave a player free to fire a gun the crew had switched off — the same defect issue #943 reported for torpedo conservation, fixed there the same way, downstream of admission where there is no source identity left to branch on."
        - "[ai] It composes into the VALUE of the existing red_alert fact rather than adding a fact or a conjunct: every armed hull authors one predicate — fact(red_alert) >= param(min_alert_to_fire) — differing only in its threshold, so a second fact would mean re-authoring the whole fleet, and a hull whose author forgot it would ignore the order in silence. Red Alert therefore cannot override cold — the cold rung is read before the alert, so raising the alert on a ship whose guns are off changes nothing."
        - "[ai] The seeded value is a LADDER, not a boolean with a sentinel. min_alert_to_fire is a floor on how hot the ship must be before a bank opens up, and a switched-off group is a rung BELOW stood-down. Sitting under every authorable floor is what makes the lever work on the always-armed Harrow gun line, which authors a threshold of 0: a cold reading seeded as a plain 0.0 would leave those hulls firing with no power in their guns, because 0 >= 0."
        - "[ai] Issue #1398 left this the ONLY occupant of the rung. The captain's ShipWeaponsHold shared it from #1041 and is now deleted, along with the SetWeaponsHold command, the console control, the weapons-hold digest namespace and the EntityState.weapons_hold snapshot field (SNAPSHOT_FORMAT 19 to 20). Two levers that had to be read together were one lever too many, and the surviving one is the one an opponent's Sensors can see — sensor-radar-blackboard-state's selected_target_weapons_cold, issue #1397."
        - "Graduated rules of engagement stay parked. There are no authority tiers and no per-target rules of engagement here; that design is out of scope under issue #834, and a review that finds either has found scope creep."
        - "AI/human symmetry is structural rather than asserted: the NPC fire hosts respect a cold group because they seed the same fact and resolve the same authored predicate, with no branch anywhere on who is flying."
        - "[ai] PowerCold is kept apart from Offline in the refusal vocabulary: Offline is a system the crew have LOST, cold is one they switched off and can switch back on with one order. The wire carries only Applied/Refused, so the distinction costs no message vocabulary."
        - "[ai] Absence fails OPEN, in both inputs: a ship with no reactor component, or a system its hull put on no power group, is not a ship with dead guns but one the question cannot be asked of. This is the same choice PowerSystem::is_group_cold makes for an untracked group, and it is what keeps the bare-App weapons fixtures — which spawn neither a ShipConfig nor a reactor — behaving exactly as they did."
    architecture:
      kind: subsystem
      authority: authoritative
      reads:
        - ship-power-state
        - ship-station-system-topology-state
      runs_in:
        - host-browser-runtime
    implementation:
      status: declared
      paths: [src/console/weapons/server.rs, src/console/weapons/mod.rs, src/console/weapons/beam.rs, src/console/weapons/blaster.rs, src/console/weapons/torpedo.rs, src/console_ai/server.rs, src/ship/config.rs]
      symbols:
        - system_power_group_is_cold
        - power_group_for
        - WeaponsAlertPosture
        - WEAPONS_COLD_ALERT_FACT
        - WeaponActionRefusal
      tests: [cargo test, tests/smoke/weapons-cold-fire-gate.spec.js]
    evidence:
      - kind: test
        reference: "src/console/weapons/server_tests.rs::a_cold_weapons_group_holds_fire_and_power_gives_it_back"
        summary: "A backfilled bank with every readiness reading favourable and the alert RAISED holds while its authored group is at level 0, and opens fire on the next AI tick after the group is powered back — one run, one thing changed."
      - kind: test
        reference: "tests/headless_runner.rs::powering_the_weapons_down_silences_an_always_armed_hull_and_restoring_it_gives_the_fire_back"
        summary: "The same claim end to end on the hard hull: probe_restraint's Harrow patrol boat, always armed at min_alert_to_fire = 0, stops firing when the scenario's hold_fire order takes its weapons group cold and resumes when release_fire restores the hull's authored default_level."

  - component: scripted-weapons-restraint-orders
    core:
      status: implemented
      confidence: confirmed
      origin: ai
      title: Scripted Weapons Restraint Orders
      summary: "The scenario's half of the restraint lever (issue #1398). The Rhai verbs hold_fire(name) and release_fire(name) kept their names and lost their own state: each pushes an ActionCmd::SetGroupPower naming the ship, the weapons group and a ScriptedPowerLevel (Exact(0) to hold, AuthoredDefault to release). The dispatch applier resolves the authored entity NAME against WorldContentRuntime::name_to_uuid and queues a PendingGroupPower; ship::power::drain_scripted_power_orders drains it through the same PowerSystem::set_group_allocation an Engineering console order reaches. mirror_weapons_cold_flags then publishes the reactor reading as the world flags weapons_cold.own_ship (role key) and weapons_cold.<name>, chained in the same tick so an on_flag_set handler fires on the next pipeline pass."
      rationale:
        - "[ai] The verbs keep their names because a scenario says hold fire, not command the weapons group to zero. Renaming them would have edited probe_picket.toml, probe_restraint.toml and Falling Skyway's three Havelock enforcer calls to say the same thing in engine vocabulary."
        - "[ai] A hull can REFUSE. The order goes through the very setter a console order does, so the group's authored [power_groups.weapons] min_level clamps it: a hull authoring 0 can be silenced and one authoring 1 cannot. Which hulls may express restraint is a designer's decision in the entity TOML rather than this vocabulary's — which is why ship_harrow_patrol.toml, the hull all three worlds order, grew a [power_groups.*] block of its own here (transcribing the canonical seeding, with min_level = 0 on weapons and power_group = \"weapons\" on its two phaser banks). It is the first NPC hull in the fleet that may be taken cold; ship_harrow_tug.toml already declares a block of its own, for its tractor bus, and what is new here is min_level = 0 on weapons."
        - "[ai] release_fire restores the group's authored default_level rather than a number the world file or the applier guessed. That resolution can only happen at the applier, which is the first place holding both the ship and its ShipConfigComponent; the Rhai closure that pushed the order has no world access at all. ScriptedPowerLevel names the two intents so the general Exact(level) form stays available to a later set_power verb."
        - "[ai] The order writes the reactor DIRECTLY rather than manufacturing an admitted command, which is the shape every other scripted world effect has. Admission is the boundary between an OPERATOR and the ship, and the world is not an operator. It is not a bypass: nothing here decides whether the ship fires, and the clamp above is the crew's own."
        - "[ai] The mirror is written off the REACTOR rather than beside the order, because the reactor has a second author — the crew's Engineering officer — and a flag pushed at the order site would have covered the scenario's orders and silently missed theirs. It replaces the retired weapons_hold.* mirror key for key; falling_skyway.toml and probe_restraint.toml re-key their on_flag_set handlers and nothing else about those beats changes."
        - "[ai] The mirror walks every ship every tick rather than filtering on Changed. ShipPowerSystem is taken by &mut each tick by tick_power_system to integrate the battery, so change detection fires for every ship anyway. The store is READ before it is written, which is the property that actually matters: a DerefMut on WorldContentRuntime marks the resource changed and change detection on it is read elsewhere, which is how issue #1041's first draft moved probe_radiation.toml's committed digest with the lever untouched."
    architecture:
      kind: subsystem
      authority: authoritative
      reads:
        - ship-power-state
      writes:
        - ship-power-state
      runs_in:
        - host-browser-runtime
    implementation:
      status: declared
      paths: [src/world/script/effects.rs, src/world/dispatch.rs, src/world/server.rs, src/ship/power.rs, src/modifiers/power_system.rs, assets/entities/ship_harrow_patrol.toml, assets/worlds/probe_restraint.toml, assets/worlds/probe_picket.toml, assets/worlds/falling_skyway.toml]
      symbols:
        - ActionCmd
        - ScriptedPowerLevel
        - PendingGroupPower
        - drain_scripted_power_orders
        - mirror_weapons_cold_flags
        - OWN_SHIP_WEAPONS_COLD_FLAG
        - weapons_cold_flag
      tests: [cargo test, "cargo test --features headless --test headless_runner restraint"]
    evidence:
      - kind: test
        reference: "src/ship/power_tests.rs::a_scripted_hold_takes_the_weapons_cold_and_release_restores_the_authored_default"
        summary: "A hull booting its weapons group at 3 rather than the fleet's usual 2 goes to 0 on hold_fire's order and back to 3 on release_fire's — so an applier restoring a hardcoded 2 would fail here and pass everywhere else."
      - kind: test
        reference: "src/ship/power_tests.rs::a_scripted_hold_is_clamped_by_the_hulls_own_authored_floor"
        summary: "A hull authoring min_level = 1 stays at 1 under the same order, and the mirror flag says so — the script gets no privilege the crew do not have."
      - kind: test
        reference: "tests/headless_runner.rs::the_crews_own_restraint_is_visible_to_the_scenario_and_something_answers_it"
        summary: "probe_restraint's operator reacts to weapons_cold.own_ship through on_flag_set, once on the transition rather than once per tick, and the named enforcer mirrors under weapons_cold.<name> beside it."

  - state: red-alert-status-replica
    core:
      status: implemented
      confidence: confirmed
      title: Red Alert Status Replica
      summary: Non-authoritative captain and viewscreen presentation of the local ship's alert state.
    architecture:
      classification: replicated
      derived_from:
        - authoritative-red-alert-state
      owner: red-alert-console-interface
    implementation:
      status: declared
      paths: [src/console/captain/server.rs, gui/console-state.js]

  - message: set-red-alert-command
    core:
      status: implemented
      confidence: confirmed
      title: Set Red Alert Command
      summary: Client-to-host request carrying the desired Red Alert state as active true or false.
      rationale:
        - The payload expresses the requested end state, so retries and duplicate delivery do not invert the alert state.
        - AI operators use the same command shape after they calculate their desired state.
    architecture:
      authority: request
      producer:
        - red-alert-console-interface
        - red-alert-ai-operator
      consumer:
        - host-command-admission
        - host-red-alert-control-router
      validator:
        - host-command-admission
      trust_boundary: client-to-host
      payload: set-red-alert-active
    implementation:
      status: declared
      paths: [src/core/messages.rs, src/core/codec.rs, gui/action-map.js, gui/sim-state.js]
      tests: [cargo test, npx vitest run]

  - component: host-red-alert-control-router
    core:
      status: implemented
      confidence: confirmed
      title: Host Red Alert Control Router
      summary: Authoritative handler that validates an admitted SetRedAlert command and assigns its active value to the addressed ship.
      rationale:
        - "[ai] The router must assign rather than invert the state."
        - It must apply commands to the owning ship entity so player and AI ships remain independent.
    architecture:
      kind: subsystem
      authority: authoritative
      owns:
        - authoritative-red-alert-state
      consumes:
        - set-red-alert-command
      writes:
        - authoritative-red-alert-state
      runs_in:
        - host-browser-runtime
    implementation:
      status: declared
      paths: [src/console/captain/server.rs, src/core/messages.rs]
      tests: [cargo test]

  - component: red-alert-ai-operator
    core:
      status: implemented
      confidence: confirmed
      title: Red Alert AI Operator
      summary: Per-ship AI operator that seeds a typed fact snapshot about recent combat activity and the nearest hostile contact, resolves the hull's authored red-alert policy over it, and submits the same explicit set command used by the captain interface.
      rationale:
        - "[ai] The operator decides nothing itself. It seeds facts and resolves the hull's authored policy, so which readings become an alert is content rather than code."
        - Recent damage, hostile fire, or own weapon use within the authored combat window is one authored reading; standing down outside it is the unconditional fallback.
        - "Issue #912 added the first-contact half. The combat timer starts only once someone has fired, and issue #872 gates a crewed hull's own weapons on the alert, so a fully backfilled Alliance hull could previously only ever return fire. The operator now also seeds a hostile-contact flag and the planar range to the nearest faction-hostile, both unconditionally, so an authored guard can open an engagement. An absent fact makes every comparison read false, which is why neither reading is seeded conditionally."
        - "[ai] The two contact readings come from the shared per-tick world snapshot rather than a scan of the operator's own, and reuse the same nearest-hostile and relative-motion helpers the Helm and Tactical consoles read, so the captain's verdict cannot disagree with the consoles that act on it. The operator runs in the input phase and the snapshot is built in the physics phase, so it reads the previous tick's frame by the project's frozen-snapshot doctrine."
        - The hulls that author no first-contact rule are the always-armed Harrow gun line and the unarmed Requiem courier; their weapons need no alert, so raising one would move every other reading keyed on alert state for no change in fire behaviour.
        - The operator is enabled by the Red Alert system's own control source, not by the umbrella Captain system.
    architecture:
      kind: service
      authority: authoritative
      reads:
        - authoritative-red-alert-state
      sends:
        - set-red-alert-command
      runs_in:
        - host-browser-runtime
    implementation:
      status: declared
      paths: [src/console/captain/server.rs, src/ai/core.rs]
      tests: [cargo test]

  - state: selected-target-alert-state
    core:
      status: implemented
      confidence: confirmed
      title: Selected Sensor Target Alert State
      summary: Read-only alert state for the contact selected on the Sensors radar, absent for targets without a Red Alert capability.
      rationale:
        - Sensors should show the selected contact's current alert state without exposing a new detail field for every radar contact.
        - The state is useful for ship contacts and remains absent for asteroids, planets, and other non-ship targets.
    architecture:
      classification: replicated
      derived_from:
        - authoritative-red-alert-state
      owner: sensor-target-alert-publisher
    implementation:
      status: declared
      paths: [src/core/messages.rs, src/ship/sensors.rs]
      tests: [cargo test]

  - component: sensor-target-alert-publisher
    core:
      status: implemented
      confidence: confirmed
      title: Sensor Target Alert Publisher
      summary: Host publication path that resolves the current Sensors target to a ship and replicates only that ship's alert state to the Sensors scan card.
    architecture:
      kind: publisher
      authority: authoritative
      reads:
        - authoritative-red-alert-state
      writes:
        - selected-target-alert-state
      runs_in:
        - host-browser-runtime
    implementation:
      status: declared
      paths: [src/ship/sensors.rs, gui/console-state.js, gui/components/ph-sensor-panel.js]
      tests: [cargo test, npx vitest run]

  - component: npc-red-alert-capability-provisioner
    core:
      status: implemented
      confidence: confirmed
      title: NPC Red Alert Capability Provisioner
      summary: NPC ship-spawn rule that ensures every behaviour-driven ship has an AI-only red-alert system, adding it when its authored ship configuration omits one.
      rationale:
        - An NPC can carry alert state today but cannot be operated by Red Alert AI unless its system configuration declares that capability.
        - The capability is mandatory for behaviour-driven ships and must be AI-controlled; authors may still declare it explicitly for clarity.
        - "Issue #871 (3394578e) left this provisioner in place but made it unexercised by shipped content. Every NPC hull now authors its own stations and its own red-alert provision, so the synthesised ownerless system is no longer reached by any shipped template; three existing tests that used shipped hulls to demonstrate it were re-pointed to inline fixtures rather than weakened, because their fixtures had stopped being examples of what they tested. The synthesis path itself is still live for a bare [behaviour] hull that authors none."
        - "Issue #871 also makes the ai_only marker on the synthesised system load-bearing in a new place. It is ownerless by construction, so no station rating can reach it; ship::rating::seed_boot_ratings therefore runs a SECOND pass that sets every ai_only system to Ai after the per-station pass. Without it the system would fall to ControlSourceResolver's Human default and silently stop being AI-operated."
    architecture:
      kind: service
      authority: authoritative
      writes:
        - authoritative-red-alert-state
      runs_in:
        - host-browser-runtime
    implementation:
      status: declared
      paths: [src/entities/config.rs]
      tests: [cargo test]

  - component: red-alert-presentation
    core:
      status: implemented
      confidence: confirmed
      title: Red Alert Presentation
      summary: Projects the authoritative alert boolean into Captain client data and host viewscreen visual treatment; the client may play the local Red Alert siren from its replica.
    architecture:
      kind: presentation-layer
      authority: derived
      reads: [authoritative-red-alert-state]
      writes: [ship-system-blackboards-state]
      renders: [authoritative-red-alert-state]
      runs_in: [host-browser-runtime, phone-browser-runtime]
    implementation:
      status: declared
      paths: [src/console/captain/server.rs, src/server/viewscreen_border.rs, gui/components/ph-red-alert.js, assets/sounds/red_alert_siren.ogg]
