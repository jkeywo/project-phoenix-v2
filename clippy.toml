# Issue #908 — native↔wasm lockstep determinism.
#
# std's float transcendentals route to the platform libm (native links the
# system one, wasm gets Rust's) and IEEE-754 guarantees nothing about their
# last bit, so a raw `.sin()` in simulation code silently desyncs peers.
# `crate::simmath` (pure-Rust `libm` crate) is the only sanctioned source —
# see src/simmath.rs. CI denies clippy warnings, so a new raw call fails the
# build. Presentation-only code opts out with an `#[allow(clippy::disallowed_methods)]`
# plus a one-line justification: audio_config.rs, entities/star.rs and
# weapons/beam_render.rs scope it per-fn at their one call site; gui/radar.rs,
# server/pfx.rs, server/renderer.rs and viewer/camera.rs scope it at module
# level (`#![allow(...)]`) because the whole module is render/presentation
# path. New sim-feeding helpers must not be added to those four modules.
#
# `sqrt`, `powi`, `to_radians`, `to_degrees`, `abs`, `rem_euclid` and plain
# arithmetic are IEEE-exact on every target and stay on std.
#
# Issue #903 — RNG entropy boundary. Every seeded sim draw already goes
# through `crate::sim_rng::with_stream` (issue #897); the entries below ban
# the OS-entropy *entry points* `rand` exposes elsewhere, so a new draw
# reaching one of them from a sim module fails the build instead of silently
# desyncing lockstep. `rand::random`/`rand::rng` are the two live entry
# points; `SeedableRng::from_os_rng` is banned pre-emptively even though
# nothing calls it today, so a future contributor reaching for
# "just seed it from the OS" hits the same wall. Deterministic seeding
# (`seed_from_u64`, `from_seed`, `Pcg32::seeded`) is NOT banned — it is pure
# and several sanctioned call sites use it (asteroids' local seeded scheme,
# constraint 8; #899's registration shuffle). Sanctioned exceptions:
# `src/sim_rng.rs` scopes a per-fn allow at `SimRng::random` (the one
# documented OS-entropy call in the sim), the `with_stream` fallback arm for
# bare-`App` fixtures, and `unseeded_test_rng`; `src/server/pfx.rs` and
# `src/server/renderer.rs` already carry a module-level allow for the f32
# entries above and it covers these too; `src/server/viewscreen_border.rs`
# scopes a per-fn allow at `apply_camera_shake` (screen-shake jitter, no sim
# effect). New sim-feeding code must not be added to any of those scopes.
disallowed-methods = [
    { path = "rand::random", reason = "sim code: use crate::sim_rng::with_stream (issue #903/#897) — cosmetic code: scope #[allow(clippy::disallowed_methods)] with a reason" },
    { path = "rand::rng", reason = "sim code: use crate::sim_rng::with_stream (issue #903/#897) — cosmetic code: scope #[allow(clippy::disallowed_methods)] with a reason" },
    { path = "rand::SeedableRng::from_os_rng", reason = "sim code: use crate::sim_rng::with_stream, not OS entropy (issue #903/#897)" },
    # f32 — the sim's float width. Everything below is a platform-libm call.
    { path = "f32::sin", reason = "use crate::simmath::sin — see src/simmath.rs (issue #908)" },
    { path = "f32::cos", reason = "use crate::simmath::cos — see src/simmath.rs (issue #908)" },
    { path = "f32::sin_cos", reason = "use crate::simmath::sin_cos — see src/simmath.rs (issue #908)" },
    { path = "f32::tan", reason = "use crate::simmath::tan — see src/simmath.rs (issue #908)" },
    { path = "f32::asin", reason = "use crate::simmath::asin — see src/simmath.rs (issue #908)" },
    { path = "f32::acos", reason = "use crate::simmath::acos — see src/simmath.rs (issue #908)" },
    { path = "f32::atan", reason = "use crate::simmath::atan — see src/simmath.rs (issue #908)" },
    { path = "f32::atan2", reason = "use crate::simmath::atan2 — see src/simmath.rs (issue #908)" },
    { path = "f32::powf", reason = "use crate::simmath::powf — see src/simmath.rs (issue #908)" },
    { path = "f32::exp", reason = "use crate::simmath::exp — see src/simmath.rs (issue #908)" },
    { path = "f32::exp2", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::exp_m1", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::ln", reason = "use crate::simmath::ln — see src/simmath.rs (issue #908)" },
    { path = "f32::ln_1p", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::log", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::log2", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::log10", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::sinh", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::cosh", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::tanh", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::asinh", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::acosh", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::atanh", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::cbrt", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f32::hypot", reason = "add a libm-backed wrapper to src/simmath.rs (issue #908)" },
    # f64 — nothing in the sim is f64 today; ban the same family so a new one
    # cannot sneak in through a wider type.
    { path = "f64::sin", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::cos", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::sin_cos", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::tan", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::asin", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::acos", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::atan", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::atan2", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::powf", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::exp", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::exp2", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::exp_m1", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::ln", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::ln_1p", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::log", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::log2", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::log10", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::sinh", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::cosh", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::tanh", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::asinh", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::acosh", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::atanh", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::cbrt", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
    { path = "f64::hypot", reason = "add an f64 libm-backed wrapper to src/simmath.rs (issue #908)" },
]
