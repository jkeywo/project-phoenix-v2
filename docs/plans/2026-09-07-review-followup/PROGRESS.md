# Execution record

User approved implementation on 7 September 2026. Integration worktree: codex/review-followup-plan. Code, documentation, profiling evidence and required validation are all part of this batch; the conditional experiments retain the stopping criteria in the approved plan.

## Current work

R1, R2a, R2b, R3, A1, A2, A3, A4, A5, G0, P1 and P2 are integrated, with subsequent regression corrections through 08b8598d. The quiet profiling window is finished and the user has been told other processes can resume. P3 was not adopted because representative hidden surfaces performed no measurable paint/upload work; P4's single Station presentation pilot did not justify a change. Reviewed cooldown, iframe scheduler and native delta-queue corrections are integrated; the latter passes 182 focused tests and corrected-SDK stationary radar acceptance. A mission-ending delivery/HUD correction passes eight focused Rust checks. Final Rust gates and the remaining native/WASM functional acceptance are pending. The branch includes main 76d36d24 and delivered #1404 commit 718d9a90.

| Slice | Tracker | Status | Integrated commit / evidence |
|---|---|---|---|
| R1 | [#1406](https://github.com/jkeywo/project-phoenix-v2/issues/1406) | Reviewed, tested and integrated; targeted native/browser restore coverage passes | 20de458e |
| R2a | [#1410](https://github.com/jkeywo/project-phoenix-v2/issues/1410) | Reviewed, tested and integrated | b7d158ab |
| R2b | [#1411](https://github.com/jkeywo/project-phoenix-v2/issues/1411) | Shared native/browser transcripts and real reconnect/channel smoke coverage pass | ac700aa0 |
| R3 | [#1407](https://github.com/jkeywo/project-phoenix-v2/issues/1407) | Native pack coverage and real browser catalogue flow pass; initial Welcome ordering fixture corrected | 47a60ec1, 88eb88c7 |
| G0 | [#1409](https://github.com/jkeywo/project-phoenix-v2/issues/1409) | Frozen headless/native baselines complete, with explicit rejected-run replacement and provenance | 21185b7e, 8d610f17, 194a057e; RESULTS.md |
| P5 | [#1416](https://github.com/jkeywo/project-phoenix-v2/issues/1416) | 18 valid named renderer/control captures, actual GPU timestamps; documented no-change decision | RESULTS.md and linked renderer attribution |
| P6 | [#1417](https://github.com/jkeywo/project-phoenix-v2/issues/1417) | 18 valid named headless/control captures plus three 120-second controls; streaming tails attributed, no simulation change | RESULTS.md and linked headless attribution |
| A1 | [#1408](https://github.com/jkeywo/project-phoenix-v2/issues/1408) | Reviewed, tested and integrated | 53663196 |
| A2 | [#1412](https://github.com/jkeywo/project-phoenix-v2/issues/1412) | Reviewed, tested and integrated | 23e77dc9 |
| A3 | [#1413](https://github.com/jkeywo/project-phoenix-v2/issues/1413) | Reviewed and integrated; owner and real continuation tests pass | 9e2fa97f plus reviewed fixture correction; final Control rerun 1/1, both cases × 12 steps |
| A4 | [#1414](https://github.com/jkeywo/project-phoenix-v2/issues/1414) | Reviewed, integrated after R2a and three registered flow tests pass | a1c8de08 |
| A5 | [#1415](https://github.com/jkeywo/project-phoenix-v2/issues/1415) | Reviewed and integrated; behavior/mint and corrected registration-inventory tests pass | 3b750738, fixture 8d610f17; native lobby 25 passing tests plus corrected inventory 1/1 |
| P1/P2/P3/P4 | [#1405](https://github.com/jkeywo/project-phoenix-v2/issues/1405) | P1/P2 integrated; 36 selected runs each plus five valid crossover controls; P3/P4 not adopted; final functional and original three-Station acceptance remain | P1 50618b63; P2 dc16185c; preserved unadopted P3 3c6341f4 |
| Pane thread | [#1404](https://github.com/jkeywo/project-phoenix-v2/issues/1404) | Integrated through rebase; follow-up acceptance still to be recorded | 718d9a90 |

A3b follows the reviewed Power pilot: both capture and restore field conversion move out of snapshot orchestration, while the old payload paths and field layout remain compatible. Performance acceptance requiring real hardware is recorded separately from automated correctness, and no unrun leg is considered complete.

The user selected separate monitors for the current rig: 1080p Viewscreen and Helm, with Tactical on the laptop panel. The current desktop query reports that panel as 1920×1200 at 125% scale (the GPU inventory reports its 3840×2400 native mode); actual host window dimensions must corroborate the capture. This replaces the original review's now-disconnected third 1080p monitor. Preserve the changed workload in conclusions.

## Branch baseline

- Planning commit: 38a97585 (was 9c1b333d before rebase).
- Main base: 76d36d24, containing #1404 718d9a90 and smoke dependency fix 4853f00c. The duplicate ec8db067 cherry-pick was dropped by rebase.
- R2b integration checkpoint: ac700aa0. The subsequent validated correction includes the A3 fixture, the headless logging borrow and removal of an obsolete A5 helper comment.
- P2/P3 remain pinned on their child refs, separate from the P1 baseline: codex/review-hud-revisions and codex/review-hidden-paint.

## Initial validation ledger (historical checkpoints)

G0 analysis: 22 focused JS tests passed; PowerShell parsers and JavaScript syntax checks passed. The native observer's three tests and the attribution example's two tests pass. Independent review corrected PDB filenames, immutable profile launch, continuous console/workload evidence and observation-only contention accounting. The maintained comparison helper uses a private target after a stale shared-worktree library was observed during A2 validation. The completed quiet-window artifacts instead used the documented one-time shared-cache helper, which forced and verified fresh project library and executable outputs while competing builds were paused. Each receipt records the actual method.

A1 source review found no production issues: 2 new-call tests, 135 World tests, 172 Comms tests and 16 engine tests passed; PASM validate/scan/traceability passed. A2 passed 212 targeted tests plus the PASM triplet and independent review. R1 source review required a real GameOver state transition in its regression, now corrected. R3 review moved real native pack installation out of the lib-test process. R2b's final review confirms shared ownership before ECS startup, immutable exact handles, stale-incarnation filtering at queue flush and codec-boundary serialization; 192 targeted JS checks pass, while native/WASM transcript and live browser reconnect execution remain pending.

A3, A4 and A5 source/test reviews and targeted Rust checks pass, as detailed below. P1 surface capture/reducer reviews and PASM triplet pass; the non-SDK pane suite passes 169 tests. P2 and P3 have independent source/test reviews, focused JS checks, formatting/wiki checks and PASM validation/scan/traceability green. P3's reducer addition passes 7/7, including hidden retention and reveal accounting. These child checkpoints still require integrated Rust/SDK tests and real-page/hardware acceptance after baseline freeze. No measured speedup is claimed. Full repository gates run once after integration before any push; later changes invalidate the affected checks.

## Combined validation checkpoint

The combined host/headless/perf build passed. Frozen test binaries report: native frame observer 3/3; registered lobby-result flows 3/3; boot parity 9/9; Power owner 2/2; Control owner 4/4; panes 169/169; attribution example 2/2; native pane integration 12/12. Power continuation passed its real resumed-simulation test. These are targeted results, not the final repository gate.

Control continuation's initial failure was diagnosed against the unchanged duel continuation, which still passes all 120 frames. Immediate restored full digests matched; the first resumed mismatch began in the entity fold. Per-ship diagnostics showed the restored NPC retained active boost while the live fixture had deactivated it. The fixture had called the complete `restore_into` operation merely to seed axes and targets, manufacturing a changed Boost command on the live ship; Bevy deliberately skips that command on a freshly inserted high-fidelity bundle. The reviewed test-only correction writes real axis/LastHelmInput/Tactical/Science components directly and retains the completed-tick drive intents and attacker/policy/recovery memory. Complete frontier equality, immediate full digest equality and all 12 subsequent frontier/full-digest comparisons remain. The final rerun passes both nondefault and cleared cases (1/1 test); no production conversion or digest coverage was changed. Evidence: [Control rerun](C:/Coding/project-phoenix-v2/.worktrees/review-followup-plan/.phoenix/control-fixture-test.log), [120-frame control](C:/Coding/project-phoenix-v2/.worktrees/review-followup-plan/.phoenix/validation-diagnostic/bounded-duel-control.log), [diagnostic values](C:/Coding/project-phoenix-v2/.worktrees/review-followup-plan/.phoenix/validation-entity-diagnostic/snapshot_resume.log).

Native lobby integration initially passed 25/26, including both new materialization behavior/mint tests. The registration-inventory fixture inspected an already-initialized graph, whose systems Bevy moves into the executable schedule. It now inspects the actual production registration before first use; that corrected test passes 1/1. This is the original 25 passing cases plus the corrected inventory rerun, not a claimed second full-suite run. Evidence: [inventory rerun](C:/Coding/project-phoenix-v2/.worktrees/review-followup-plan/.phoenix/validation-diagnostic/native_host_lobby.log).

The profiling reducers pass 22 JS tests. Review added a shared frame/surface origin, exact interval reduction, and proof of a loaded, uploaded, continuously live HUD. A static HUD remains valid without repeated measured applications or uploads. The separate named renderer example explicitly records no surface attribution. Rust profiling serialization goes through core::codec; read-only review found field names, values, nullability and nested payloads preserved. A later diagnostic build exposed a temporary-borrow error in the headless logging macro; the working-tree correction compiles with the final Control test rerun. R2b validation and release/SDK artifact preparation remain separate pending work. No fresh profiling artifact or measured speedup is claimed.

## Subsequent focused validation

At 07c693d4, `cargo test --target-dir C:/Coding/project-phoenix-v2/target --features host,headless,perf --lib session_connections::browser::tests:: -- --test-threads=1` passed 2/2. The frozen executable from that build passed `native_adapters_follow_the_shared_browser_ownership_transcript` (1/1) and `perf::native_frames::tests::` (3/3), including the updated codec serialization probe. Evidence is under `.phoenix/validation-r2b/` and `.phoenix/r2b-browser-cached-*.log`. An earlier filter mistakenly applied to the pane integration executable matched zero tests and is not counted; an accidental cold dependency build was stopped before restarting against the existing cache.

A fresh release WASM build uses Trunk 0.21.14, wasm-bindgen 0.2.120, the shared target directory and `NO_COLOR=true` (Trunk refuses the tool shell's inherited `1`). Its planned browser set is 33 tests across restore/save, ownership/reconnect, snapshot channels and real mod-pack/catalogue coverage. No browser success is claimed before execution.

The final launcher audit corrected five relative-output resolutions to use PowerShell's current provider location. All five scripts pass parsing, and an actual throwaway client preparation with relative source/destination after `Push-Location` placed the instrumented bundle at the intended location. Source review found no other current-launch blocker; SDK startup, continuous readiness evidence and hardware comparisons remain pending. The updated implement-issues skill governs remaining work: this task owns integration/final gates, existing evidence is reused while inputs remain unchanged, and workers do not push or repeat broad gates.

## Completed profiling and later corrections

All controlled captures are finished: the two 36-run native matrices, five crossover controls, six release headless runs, eighteen named headless runs, eighteen named renderer runs and three 120-second Combat controls. RESULTS.md records selected runs, exclusions, timings, provenance and conditional stop decisions. P2 is integrated at dc16185c; P3 and P4 changes were not adopted. The user was told the quiet window could end before any later functional work.

The release WASM build at 194a057e succeeded with binary SHA-256 8ad6462a00c235b438f4252be5e3ee1e51d9acc95b30c6e17d8cdde9b9047258. The targeted real-browser set passed 32/33 initially; the corrected catalogue fixture passed its 1/1 rerun at 88eb88c7. These checks cover restore/save, connection ownership, reconnect and snapshot channels, real mod-pack and catalogue flow, but contain no renderer cases. The final core/render browser gate will refresh both host GUI and the phone bundle while reusing this WASM artifact: subsequent production changes are native-only or JS. Its original Rust revision remains explicit.

The reported cooldown display used a binary full/empty width despite receiving authoritative remaining durations. ccf54984 joins the existing Welcome bank duration by bank ID and renders the clamped remaining/duration fraction, preserving command readiness and the old fallback when duration is unavailable. Six focused JS suites passed 608/608. A real native combat diagnostic subsequently observed changing phaser/blaster remaining values and proportional rendered bar widths.

cfa18a77 shares native-pane animation scheduling with actual Station component iframes. The targeted suites passed 64 cases plus the corrected radar-coordinate assertion on its 1/1 rerun; the source was unchanged between runs. The controlled old-SDK radar fixture did not reproduce the user's stationary freeze, so this correction alone is not claimed as the confirmed cause or cure. Both client fixes passed independent review.

af0b4609 corrects native queue data loss: BlackboardUpdate batches compose by System key and SimState sparse fields by entity UUID; whole blackboard privacy withdrawals replace their prior values. Complete state replaces only the same kind, and reliable/events are ordering barriers. No final change-only projection is silently shed at capacity: the existing overflow path closes the pane for the operator to reopen. The corrected focused command `cargo test -p project-phoenix --jobs 6 --features host,headless,perf --lib native_host::panes:: -- --test-threads=1` passed 182/182 using the shared target and a freshly compiled project library. Independent review passed after correcting capacity shedding. The first run had 179 passes and one obsolete fixture failure, which used lifecycle events as fake replaceable snapshots; that fixture now uses real complete message variants. Evidence is under `.phoenix/native-delta-validation/`.

The frozen comparisons do not measure these later animation and queue fixes. A fresh SDK build at 2ce2cce6 succeeded, with executable SHA-256 72cd62f3db13fe37d6a778455ab9437320d0e00548216544c219708d7b797b31. The separate instrumented functional run `final-native-combat-02` demonstrated stationary Helm over elapsed 90–160 seconds: exact unchanged planar position/heading, zero forward/lateral speed, live snapshots, moving real contacts and advancing visible radar canvases on both Stations. F9 reveal/hide and genuine red-alert changes also passed visually. Neither weapon family entered cooldown in that run, so the earlier combat cooldown evidence remains separate. No comparative timing claim follows from this run.

The same run exposed a genuine terminal defect at tick 19726: the Viewscreen dimmed with empty ending text and both Stations stayed InProgress. Source review found GameOver waiting in the InProgress-only simulation outbox, plus an unordered HUD reader racing consumption of the ending reason. A real browser regression against the frozen 194a057e WASM reproduced both defects in a scripted ending: its visible HUD had empty text and no reliable GameOver arrived. The independently reviewed correction is integrated at 08b8598d: it routes the ending directly to the frame-driven transport and orders final HUD capture before reason consumption, retaining snapshot/digest semantics. Five outcome/report/narrative unit cases, one registered native App/transport/HUD transition and two saved-game ending cases pass. The fresh shared-target build took 6m43s and froze all three test executables before execution; `.phoenix/lifecycle-regression/` records the exact source patch, command, artifacts and results. PASM validate/scan/traceability and the affected wiki review also pass. Corrected SDK and WASM acceptance remain pending. Other evidence is under `.phoenix/final-gates/game-over-browser-red/`, `.phoenix/final-gates/lifecycle-pasm/` and `.phoenix/wiki-lint/lifecycle-report.md`. The native run's full evidence and diagnostic-loss limits are in `.phoenix/final-native-combat-02/analysis.md`.

At the same production checkpoint, Vitest passed 6,785 tests in 286 files; debug-surface drift, strict strings, LODs, billboard captures and mod-pack fixture checks passed. PASM validate/scan/traceability passed after retrying an initial environment-level cache access failure. Wiki lint passed with two current line references corrected. The final browser core run initially passed 63/66; all three ownership-fixture failures passed their focused retries. Seven related ownership cases passed 6/7 initially, then the remaining GM takeover/release case passed 1/1 after selecting the actual host overlay rather than a shadow-root map overlay. These test-only corrections are integrated at b55abb42. Combat default rendering passed in core; Combat HDR-off and Falling Skyway rendering passed 2/2 separately. The WASM artifact remains explicitly pinned to 194a057e. This records initial runs plus focused retries, not a repeated full-suite result. Evidence is under `.phoenix/final-gates/`.

The final format check found one multiline assertion formatting difference, corrected at d59a9eb2; the full format retry passes. Other Rust gates and batch push remain pending. The shared lifecycle correction requires fresh SDK and WASM acceptance; unrelated completed checks remain reusable while their inputs are unchanged.

The subsequent 180-second all-AI SDK run `final-native-all-ai-01` completed normally with 580 fresh, visible Station samples and 289 simultaneous confirmed-AI pane samples. Eight phaser and sixteen blaster sequences each contain at least three distinct decreasing interior cooldown fractions followed by zero, with proportional inline and progressing computed widths. All 295 populated HUD samples match heading/hull DOM and alert CSS; heading has 149 distinct values, hull changes from 100 to 98, and alert toggles five times. The existing 2ce2cce6 SDK was reused deliberately: subsequent production changes affect mission endings, not these in-play paths. This run does not validate the terminal correction.

A separate 180-second `final-native-visual-01` run completed normally and supplied physical-window screenshots of partially filled phaser and blaster bars, later empty bars, changing radar contacts, HUD heading/hull and red-alert changes, and F9 reveal/hide. Its analyzer independently records seven phaser and sixteen blaster countdown sequences. Evidence is in each run's sibling `-analysis` directory and `final-native-visual-01/visuals/`. Neither sampled diagnostics nor screenshots establish continuous animation smoothness or a console frame rate. Source review confirms a phaser's `on_cooldown` also covers an active beam, while `cooldown_remaining` starts after that beam ends; numeric completion does not promise immediate readiness. The misleading wire-field comments are corrected without runtime changes.
