{
  "name": "project-phoenix",
  "private": true,
  "scripts": {
    "test": "cargo check",
    "typecheck": "cargo check",
    "build:server": "trunk build",
    "build:client": "trunk build --config client-trunk.toml",
    "build": "npm run build:server && npm run build:client"
  }
}
