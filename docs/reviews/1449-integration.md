# Integration closeout map for #1449

Prepared against local main after the issue commits. This file records decisions
and prerequisites; final gate results will be appended before push.

## Closure rules

After required integrated gates and push, #1248, #1244, #1181, #1238, #1241 and
#1269 have concrete completion evidence in their per-issue receipts. The #1269
closure records recovered current behavior, not an invented causal fix.
#1239/#1055/#1056 have current passing asset measurements; headless timing
#1247/#1272 needs the separate release/matching-runner result.

#1356 uses the owner's explicit approval to retint the remaining inline UI with
existing tokens and accept visual changes. #1357 carries the accepted fire
contrast correction. Historical screenshots were not recovered. #1359's inventory
is retrospective and cannot be called pre-work. Under #1449 A2's explicit
fallback, current render evidence and these historical/process dispositions
replace an impossible claim of recovered evidence. Missing historical evidence
is not labelled PASS. Parent #1355 closeout must retain these qualifications.

#1179 may reconcile its completed organisation work with #1201's existing human
DEFER (23 August), without claiming a delivered render/headless crate split or
its two-arm CI measurements. #1183/#1240 remain superseded by #1400.

## Outstanding acceptance and external state

- #1126: real capture/play correspondence, permission changes, unplug and
  multi-device contention. Native enumeration/refusal checks are supporting evidence.
- #1127/#1128: real Windows adoption and native focus/reflow judgment; this rig
  has only one monitor, so the native layout diagnostic explicitly skipped.
  #1422 still blocks a completed 200% T3 claim.
- #1320/#1323: two equal human GMs, ordinary crews on the prepared Fleet hulls,
  and two physical gamepads. Keep #1273/#1324 and broader parents dependent on
  their own accepted human results; no ready/accepted label from generated files alone.
- #1044/#1352: competent human crew, three-person doubling, per-act/console
  engagement and narrative acceptance. The 94-test suite and single Backfill
  narrative are not replacements.
- #855/#853: the live demo cache failure and missing client stamp are recorded.
  #1449 A4 expressly limits this to read-only evidence recovery; publishing,
  reconfiguration and release decisions remain with the deployment owner.

Unrelated pre-existing wiki and planning changes remain unstaged throughout.

## Integrated evidence

The ordinary JavaScript/PASM gate batch completed on local main. The first
Vitest run caught an obsolete bridge-source assertion in `gm-local-projection`;
commit `ffcf177b` follows the browser-edge owner and checks the source slice is
nonempty. The full rerun passed **310 files / 7,237 tests**, exit 0. Debug-surface
drift, strict strings, 44 generated LOD outputs, 22 capture outputs, PASM validate,
scan and traceability each exited 0. PASM validate reported Status OK; its
informational findings were not relabelled failures or silently removed.

The exact release workload (`--sim-seconds 60`, default world) finished after a
26m16s release build. `sim.run` mean was **7.3147143 s**, +42.2% against 5.144825412;
`sim.tick` p95 was **1.9696 ms**, +26.1% against 1.561372. Both are below the
unchanged +50% warning threshold. The report tool exited 0. This is a Windows
desktop measurement, not the matching Linux runner evidence needed to discharge
#1247/#1272. No baseline adoption occurred. Summary and capture hash are in
`1449-release-performance.json`; raw capture is retained under `target/issue-1449/`.

Final wiki lint: **64 pages / 62 indexed content pages / 1,695 references /
20 numbered references / zero errors**. Independent review passed the three
media-diagnostic lint corrections in `0a4801aa`; the full Clippy feature matrix
then passed. The full native workspace suite exited 0: **8,063 passed, 0 failed,
99 ignored across 85 executable/doc-test summaries**. This includes 7,427 main
library tests and 132 ordinary headless-runner cases. The explicitly selected
GM topology precheck then passed **1/1, 0 ignored**, in 1.06s on `1f8fc0e6`,
retaining digest `613cccc16d5570e0` on both peers over 90 compared rounds.
Viewer configuration: **34 passed, 0 failed**. Demo configuration with
`PHOENIX_DEMO_BUILD=true`: **11 passed, 0 failed**, including absent debug/pause/
console-latency routes. The previous environment value was restored. The host
binary and capture-tool builds also exited 0. A final formatter check found one
line-wrap difference in the lint-corrected tone expression; `5df5a85c` applies
only that wrapping, and the formatter then passed. `1449-local-gates.json`
consolidates the successful gates and retained retry history. The explicit real
Ultralight test also passed: **1 passed, 0 failed, 0 ignored**, 13.56s on the
`5df5a85c` source tree. Two served console pages joined, painted 1,152,000 pixels,
round-tripped a name edit and a real Red Alert control, retained separate stores,
and recovered a crashed pane on the same identity. The test's rendererless Bevy
fixture logged missing mesh/image loaders; this is a console-page/transport/
storage/recovery result, not a 3D scene-render or physical-monitor judgment.

Additional #1044 preparation in `e683a2e2` supplies all **357 mission-owned
bracketed strings** in `docs/acceptance/1044-skyway-copy-inventory.md`, grouped by
surface with source lines and hashes. Generation and `--check` passed;
independent review passed. Human approval remains NOT RUN. A full unmodified
browser timeline check is running separately, with Backfill and ordinary
spectators; it will not replace per-console human judgments.
