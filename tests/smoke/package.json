{
  "name": "project-phoenix-smoke-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:core": "playwright test --grep @core",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.2.4"
  }
}
