# Foreign admitted-command consumer proof

## Declared-order stability source revision

The current test source replaces the historical opposed-order roles below with
ordinary registration, `RegistrationOrder::Shuffled(17)` and `Shuffled(991)`;
the publisher proof also retains a separate physics-last role. Each role runs
in two fresh default-pool processes and one pinned process. These are existing
headless test seams, with no shipping flag or test-created ordering edge.
Execution of this revision is pending; the results below remain historical
receipts for their original source, not validation of this revision.

The expected directions come from the frozen actual execution inventory in
`tests/fixtures/determinism/declared-owner-order.json`. Every selected conflicting
pair must follow that direction in every role while retaining its complete raw
access vector. Full gameplay, state, wire, RNG/mint and positive coverage
assertions remain. This now proves declared-order stability, not commutativity.

Raw system instances, containing-set nodes/hierarchy and effective edges remain
in every report. Concrete IDs
may change with registration: comparison requires a unique complete metadata
correspondence including the actual ancestor-set semantic membership multiset,
rejects any still-ambiguous repeated concrete identities, and retains
all prior concrete dependency paths and deferred writer-to-consumer visibility
obligations. Automatic ApplyDeferred instances are retained in raw output but
are not assigned a cross-registration identity. Anonymous condition-set allocation
numbers also remain raw; correspondence uses their actual descendant metadata
multiset and ordered condition names, preserving distinct set multiplicity. The producer's exact eight
probes retain their individual admission/before/producer/after assertions;
incidental probe/barrier paths remain diagnostic output. Physical external
conflict vectors and multiplicities remain compared even when declared order
removes those rows from the reported ambiguity census.

## Historical proof and receipts

This prepared test covers five producer/consumer pairs from the exact 275-pair
foreign-payload inventory. The first native run exposed unstable Tactical region
ordering; the production projection now sorts by UUID. The reserved-torpedo
loading fix also orders nine historical load-consumer candidates through Bevy's
shared deferred flush. This fixture retains all 275 original identities and
their complete raw access vectors, requires the exact consumer-before-producer
direction for those nine pairs, and continues to require the remaining
unselected ordinary pairs to be unordered. No pair gains an ambiguity annotation
or allowance exemption. The corrected native run passed all nine children and
their parent, including the full observations and updated graph expectations.

The producer is the ordinary backfilled Captain's Cinematic view request. The
five consumers own Red Alert, Tactical target selection, torpedo volley size,
phaser mode and phaser frequency. Each family runs three boundaries: the foreign
view request alone; two ordered matching commands together with that request;
then an inverse command or an actual offline/missing-target refusal, again with
the foreign request. The real producer must emit exactly once at every boundary
and the real viewscreen consumer must apply it. The matching consumer must change
its actual state, retain its own command subsequence and produce its existing
correlated feedback. The frequency consumer intentionally has no feedback API;
its offline case must retain the previous frequency without a fabricated reply.

The ordinary full App supplies the authored destroyer, actual admission drain,
AI snapshot cadence, simulation consumers and publication pipeline. The fixture
uses the accepted-command continuation entry point to stage ordered matching
commands; it does not claim to test network admission or human seat authority.
Other authored fine systems are held Human and the ship/witnesses are placed
away from scenario combat. The tested producer's real snapshot latch is awaited,
never overwritten. Existing hull values, projectile rules and cadence rates
remain unchanged.

One parent starts nine fresh children: ordinary, producer-first and
consumer-first ordering, each under two fresh default pools and a pinned pool.
The graph helper refers to unique actual system instances through their existing
interned type sets. Empty test-only sets impose the opposed orders. Every
audited pair must retain the exact sole AdmittedCommands access; all five tested
pairs must have the intended effective paths and ordinary census multiplicity.
The helper retains all other actual raw conflict vectors, full reported census
and existing path multiplicity. Forced test paths may induce additional
transitive paths; those receipts do not license a production exemption.

Across children, every observed tick must have equal consumer state, complete
local blackboards, viewscreen state, RNG/mint state, canonical digest, ordered
command log, per-target admitted subsequences, balance events and raw outbound
payloads/delivery classes. Raw cross-target append order is separately retained;
it is not treated as gameplay order. All fifteen boundary cases are nonvacuous,
and six further ordinary ticks check continuation. A future annotation needs
review of the exact named pairs against these results; this fixture grants none.

The 2026-09-08 R4 run used base `a57bcca4` plus this fixture and the Comms drain
ordering fix, with `host,headless,perf,ultralight`, six build jobs, and offline
locked dependencies. Its unchanged runtime source inventory has SHA-256
`3E6C2D893A3ABD0DFC83686830AAB6DFBAB98174F2BA063FD699A424F2CA567E`.
Complete source, process, trace and graph receipts are retained locally under
`.t2-batch/rebase-focused-native-r4/`. The ordinary headless and final pre-push
matrix remain separate from this focused SDK-enabled native result.

The other 270 inventory pairs remain outside behavioral coverage; nine now have
explicit checked ordering paths and 261 remain unordered. All producer/producer
pairs remain outside this fixture.
The two excluded Helm impulse/axis interactions and all multi-access census rows
remain outside this test. Root owns prerequisite integration and one coordinated
native build after independent source review; no per-pair builds are needed.
