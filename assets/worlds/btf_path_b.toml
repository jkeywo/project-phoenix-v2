# btf_path_b.toml — Scholar branch: Dr. Myst at Research Outpost
# Loaded when the crew hails Research Outpost (root world trigger: on_hailed Research Outpost).
#
# Dr. Myst's briefing was migrated upstream into the parent world's
# `[[comms]]` block on Research Outpost (Issue: Comms follow-on messages):
# the Research Outpost root template now carries a `[comms.follow_up]`
# that auto-fires Dr. Myst's introduction 3 seconds after the hail
# without requiring this sub-world load to inject the same content.
#
# This file is retained as the load_world target so the existing
# branch-loading trigger in `before_the_fire.toml` keeps a place to
# attach future Scholar-branch entities, triggers, or sub-world
# objectives. It is intentionally empty.
