{
  "name": "project-phoenix",
  "private": true,
  "scripts": {
    "test": "cargo check",
    "test:editor": "vitest run",
    "typecheck": "cargo check",
    "build:server": "trunk build",
    "build:client": "node scripts/build-client.mjs",
    "build": "npm run build:server && npm run build:client"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "smol-toml": "^1.6.1",
    "vitest": "^4.1.6"
  }
}
