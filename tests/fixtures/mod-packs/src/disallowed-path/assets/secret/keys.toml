# NOT a supported authored path: `assets/secret/` is outside the mod-pack
# whitelist (assets/worlds|entities|factions|models). Its mere presence in the
# archive rejects the whole pack with `disallowed-path`.
danger = true
