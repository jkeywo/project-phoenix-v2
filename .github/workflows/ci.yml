name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── PASM model validation ───────────────────────────────────────────────────
  pasm:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Shared step, fleet-standard: uv setup, uv sync, pasm validate, pasm scan.
      # Keeps this repo's invocation from drifting from the rest of the fleet.
      - name: Validate PASM model
        uses: jkeywo/vellum/actions/pasm-validate@582a61206d39c13d051d2e73bf463bb5d167d484
        with:
          spec-path: pasm/spec
          scan: gate

      # Not covered by the composite action: traceability is phoenix's own
      # report, and the artifact shape below is consumed by this repo's tooling.
      - name: Generate revision-linked PASM reports
        run: |
          mkdir -p target/pasm
          uv run pasm scan --json > target/pasm/scan.json
          uv run pasm traceability --json > target/pasm/traceability.json

      - name: Upload PASM reports
        uses: actions/upload-artifact@v4
        with:
          name: pasm-reports
          path: target/pasm/
          retention-days: 14

  # ── Unit tests (native, fast — fail-fast before expensive WASM build) ─────────
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Shared step: toolchain + cache. rust-toolchain.toml pins the channel at
      # cargo time, so this installs components and warms the cache. The action
      # exposes no shared-key; rust-cache's default key is per-job, and each of
      # these keys had exactly one job, so the caches stay separate as before.
      - name: Install Rust and cache
        uses: jkeywo/vellum/actions/setup-fleet-rust@582a61206d39c13d051d2e73bf463bb5d167d484

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libudev-dev libxkbcommon-dev libasound2-dev

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Clippy (deny warnings)
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run unit tests (server)
        run: cargo test

      # The headless scenario suite (issue #912). `tests/headless_runner.rs` is
      # `#![cfg(all(feature = "headless", ...))]`, so the bare `cargo test`
      # above compiles it to NOTHING — the whole file is empty without the
      # feature. `--features headless` appeared exactly once in this workflow
      # and only as a `cargo build --bin`, never a test target, so these tests
      # had never run in CI and did not gate #872: two backfilled Alliance
      # hulls stopped shooting at each other entirely and every job stayed
      # green.
      #
      # It belongs in `test` rather than a job of its own: `test` is the
      # fail-fast gate `build`, `smoke` and `perf` all `needs:`, and it already
      # has the toolchain, the rust-cache and this exact apt set. Debug is
      # correct — the runner is deterministic and asserts on end state, not on
      # wall-clock timing — and release would triple the build for nothing.
      # Roughly 110 s on a warm cache.
      - name: Run headless scenario tests
        run: cargo test --features headless --test headless_runner

      # The seeded-RNG determinism guard (issue #837). `tests/rng_determinism.rs`
      # is `#![cfg(all(feature = "headless", ...))]`, same as the headless
      # scenario suite above, so the bare `cargo test` above compiles it to
      # nothing and only `headless_runner` was ever wired into this job — this
      # file ran on nobody's machine but the author's (found during #915's gate
      # pass). It gets its own step, mirroring `headless_runner`'s own
      # process-isolation requirement: Cargo gives each integration-test file
      # its own process, and this test needs that isolation to keep Bevy's
      # process-global task pool single-threaded and system order fixed — see
      # the file header for why sharing a binary makes the assertions flaky.
      - name: Run RNG determinism tests
        run: cargo test --features headless --test rng_determinism

      # The registration-order determinism guard (issue #899, ordering half of
      # the parent's #849 ask). `tests/registration_order_determinism.rs` is
      # `#![cfg(all(feature = "headless", ...))]`, same as the two suites
      # above, and needs the same own-process isolation for the same reason:
      # `--deterministic` only pins Bevy's task pool to one thread if nothing
      # else has already claimed the process-global pool first. Same seed, the
      # 13 SimSet-chain plugins registered in a deterministic shuffle instead
      # of their canonical order, byte-identical digest required either way.
      - name: Run registration-order determinism tests
        run: cargo test --features headless --test registration_order_determinism

  # ── WASM build ────────────────────────────────────────────────────────────────
  build:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Shared step: toolchain + cache. The wasm target is also declared in
      # rust-toolchain.toml; passed here too so the action is self-describing.
      - name: Install Rust and cache
        uses: jkeywo/vellum/actions/setup-fleet-rust@582a61206d39c13d051d2e73bf463bb5d167d484
        with:
          targets: wasm32-unknown-unknown

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libudev-dev libxkbcommon-dev libasound2-dev

      - name: Install Trunk
        uses: jetli/trunk-action@v0.5.1
        with:
          version: v0.21.14

      # Pre-install wasm-bindgen-cli at the exact version pinned in Cargo.lock.
      # Trunk normally fetches this binary from github.com/rustwasm/wasm-bindgen/releases
      # at build time, which intermittently 504s and fails the whole pipeline.
      # taiki-e/install-action uses cached/mirrored binaries with retry+fallback,
      # and Trunk will detect the pre-installed binary on PATH and skip its own
      # download. The version must match the wasm-bindgen crate version in Cargo.lock.
      - name: Install wasm-bindgen-cli
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-bindgen-cli@0.2.120

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Needed before the trunk build: its post_build hook (Trunk.toml,
      # scripts/wasm-opt-fixup.mjs) runs `node` and requires the `binaryen`
      # package to do the -Oz size optimization trunk's own wasm-opt can't
      # validate on this toolchain (see script header for why).
      - name: Install Node dependencies
        run: npm install

      - name: Build server page (WASM + JS)
        run: TRUNK_BUILD_RELEASE=true trunk build --release

      - name: Build client page (pure JS — copies client.html + gui/assets, no WASM)
        run: node scripts/build-client.mjs

      - name: Verify dist contents
        run: find dist/ -type f | sort

      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 1

  # ── Editor tests (Vitest) ─────────────────────────────────────────────────────
  editor-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install Node dependencies
        run: npm install

      - name: Run editor tests
        run: npx vitest run

      # Guards the string table: every t()/data-i18n id resolves to a CSV row,
      # no duplicate ids, every localisable TOML key holds an id rather than
      # prose, and (--strict) no new hardcoded display text sneaks into the
      # client. Without this a renamed id silently leaves a console rendering
      # ⟨some.id⟩, which no other job would catch.
      - name: Check string table
        run: node scripts/check-strings.mjs --strict

      # Guards the generated LOD models against silent drift (issue #919):
      # re-hashes every generated .glb, the source it was decimated from, and
      # the parameters its sidecar declares, and compares all three against
      # scripts/lod-manifest.toml. It deliberately does NOT re-run
      # gltf-transform — the simplifier is not byte-reproducible across CLI
      # versions, so a regenerate-and-diff gate would go red for reasons that
      # are not drift, and the runner would need the whole texture toolchain.
      # Regenerating stays a local command (`npm run lods`); this step only
      # asks whether what is committed is what the sidecars describe.
      - name: Check generated LODs are current
        run: npm run lods:check

  # ── Smoke tests ───────────────────────────────────────────────────────────────
  smoke:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: tests/smoke/package-lock.json

      - name: Install Node dependencies
        working-directory: tests/smoke
        run: npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ hashFiles('tests/smoke/package-lock.json') }}

      - name: Install Playwright browsers
        working-directory: tests/smoke
        run: npx playwright install --with-deps chromium

      - name: Run smoke tests
        working-directory: tests/smoke
        run: npx playwright test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: tests/smoke/playwright-report/
          retention-days: 7

      # Handed to the perf job, which has the Rust toolchain that owns the
      # comparison (issue #868).
      - name: Upload browser perf capture
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-browser-capture
          path: target/perf/browser-capture.json
          if-no-files-found: warn
          retention-days: 14

      - name: Publish results to job summary
        if: always()
        working-directory: tests/smoke
        run: |
          echo "## Smoke Test Results" >> $GITHUB_STEP_SUMMARY
          node - <<'NODE' >> "$GITHUB_STEP_SUMMARY"
          const fs = require('fs');
          const path = '../../target/playwright-results.json';

          if (!fs.existsSync(path)) {
            console.log('No Playwright JSON result file was produced.');
            process.exit(0);
          }

          const report = JSON.parse(fs.readFileSync(path, 'utf8'));
          const stats = report.stats || {};
          console.log(`- Expected: ${stats.expected ?? 0}`);
          console.log(`- Failed: ${stats.unexpected ?? 0}`);
          console.log(`- Flaky: ${stats.flaky ?? 0}`);
          console.log(`- Skipped: ${stats.skipped ?? 0}`);

          const failed = [];
          function walkSuite(suite, parents = []) {
            for (const spec of suite.specs || []) {
              const title = [...parents, spec.title].join(' > ');
              for (const test of spec.tests || []) {
                const bad = (test.results || []).find((r) => r.status === 'failed' || r.status === 'timedOut');
                if (bad) {
                  failed.push({
                    title,
                    project: test.projectName,
                    status: bad.status,
                    error: bad.error?.message || '',
                  });
                }
              }
            }
            for (const child of suite.suites || []) {
              walkSuite(child, [...parents, child.title].filter(Boolean));
            }
          }
          for (const suite of report.suites || []) walkSuite(suite);

          if (failed.length) {
            console.log('');
            console.log('### First failures');
            for (const item of failed.slice(0, 20)) {
              const project = item.project ? ` [${item.project}]` : '';
              console.log(`- ${item.status}${project}: ${item.title}`);
              if (item.error) {
                console.log(`  ${item.error.split('\n')[0]}`);
              }
            }
          }
          NODE

  # ── Performance measurement (issue #868; gating decided in #905) ─────────────
  #
  # Runs after smoke so the browser capture that job produces can be compared
  # here, where a Rust toolchain exists. The comparison has exactly one
  # implementation — `phoenix-perf report` — rather than a second opinion
  # written in whatever language each job happens to have.
  #
  # WHAT GATES (issue #905, and src/perf/mod.rs carries the rule):
  #
  #   assets       GATES. Bytes on disk and counts in authored TOML are a
  #                function of the checkout, not of this runner, and the
  #                runner's own capture compares at +0.0% drift. The last step
  #                below re-runs its report with --gate, which exits 3.
  #   assets-mesh  Reports. Machine-independent too, but its baseline was
  #                recorded on a developer desktop; it gates once a runner has
  #                recorded it — adopt the rendered baseline this job uploads,
  #                then add --gate to the same step.
  #   headless-default, browser-automation
  #                Report only. Wall-clock on a shared runner is moved by
  #                neighbours, and a gate that fires on a neighbour gets
  #                disabled rather than obeyed. Reviewed again post-demo.
  #
  # Still not in `deploy`'s `needs`: a gated asset regression turns this run
  # red, which is the point — it must be fixed rather than routed around — but
  # it does not hold up a deploy of the page.
  #
  # The job also renders the baselines it WOULD record from its own captures
  # and uploads them, because CI cannot commit. `phoenix-perf adopt` turns them
  # into a reviewable diff on a developer's machine — see src/perf/baseline.rs.
  perf:
    needs: [test, smoke]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust and cache
        uses: jkeywo/vellum/actions/setup-fleet-rust@582a61206d39c13d051d2e73bf463bb5d167d484

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libudev-dev libxkbcommon-dev libasound2-dev

      # Release, because a debug-build measurement describes a binary nobody
      # ships. The committed timing baseline was recorded the same way.
      - name: Build headless runner and perf tool
        run: |
          cargo build --release --features headless --bin phoenix-headless
          cargo build --release --features perf --bin phoenix-perf

      - name: Measure the default headless scenario
        run: |
          mkdir -p target/perf
          ./target/release/phoenix-headless \
            --sim-seconds 60 \
            --report target/perf/run-report.json \
            --perf-capture target/perf/capture.json \
            2> target/perf/headless-report.txt
          cat target/perf/headless-report.txt

      # No simulation runs: this reads bytes on disk and counts in authored
      # TOML, so it is machine-independent and its tolerances are tight.
      # Reported here and GATED in the last step, so a failure still leaves a
      # full artifact behind to diagnose it from.
      - name: Measure asset budgets
        run: |
          ./target/release/phoenix-perf assets --capture target/perf/assets.json
          ./target/release/phoenix-perf report --capture target/perf/assets.json \
            > target/perf/assets-report.txt
          cat target/perf/assets-report.txt

      # The mesh interior, through Bevy's own asset loader rather than a second
      # glTF reader (issue #905). No window and no renderer — the loader runs
      # and the meshes and images it produced are counted. Models are loaded one
      # at a time, so peak memory is one model rather than 150 MB of decoded
      # texture.
      - name: Measure the mesh interior
        run: |
          ./target/release/phoenix-perf mesh --capture target/perf/mesh.json
          ./target/release/phoenix-perf report --capture target/perf/mesh.json \
            > target/perf/mesh-report.txt
          cat target/perf/mesh-report.txt

      - name: Download browser capture
        uses: actions/download-artifact@v4
        with:
          name: perf-browser-capture
          path: target/perf/
        continue-on-error: true

      # `browser.frame` from CI is the ECS schedule under WebDriver, where the
      # render stack is skipped — see tests/smoke/perf-browser.spec.ts. The
      # capture's runtime says so, which is what stops it being read as a
      # rendering number.
      - name: Compare the browser capture
        run: |
          if [ -f target/perf/browser-capture.json ]; then
            ./target/release/phoenix-perf report \
              --capture target/perf/browser-capture.json \
              > target/perf/browser-report.txt
            cat target/perf/browser-report.txt
          else
            echo 'no browser capture produced by the smoke job' \
              > target/perf/browser-report.txt
          fi

      # A baseline belongs on the machine that compares against it, and this
      # runner cannot commit — so it renders the baselines it would record and
      # ships them in the artifact. Seeded from the committed files first, so
      # every rendered baseline carries the prose and the tolerances a human
      # wrote; only the measured numbers move. See src/perf/baseline.rs for the
      # adoption command that turns these into a diff.
      # continue-on-error, because this is a convenience for a human, not a
      # gate: a run where no capture survived should fail on the measurement
      # that died, not on the step that had nothing left to record.
      - name: Render the baselines this runner would record
        if: always()
        continue-on-error: true
        run: |
          mkdir -p target/perf/baselines
          cp perf/baselines/*.ron target/perf/baselines/ 2>/dev/null || true
          ./target/release/phoenix-perf adopt \
            --artifact target/perf \
            --out-dir target/perf/baselines

      - name: Summarise
        if: always()
        run: |
          {
            echo '## Performance'
            for report in headless assets mesh browser; do
              echo
              echo "### ${report}"
              echo '```'
              cat "target/perf/${report}-report.txt" 2>/dev/null || echo 'no report produced'
              echo '```'
            done
            echo
            echo 'Baselines this runner would record are in the `perf-capture`'
            echo 'artifact under `baselines/`; adopt them with the command in'
            echo '`src/perf/baseline.rs`.'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload captures
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-capture
          path: target/perf/
          retention-days: 14

      # Last, so a red gate still leaves the full artifact and summary behind.
      # `assets` is the one scenario that has earned this: machine-independent
      # by construction, and this runner reproduces the committed numbers
      # exactly. `phoenix-perf report --gate` exits 3 on a fail or on an
      # incomparable metric — a budget that could not be checked is not a
      # budget that passed.
      - name: Gate on the machine-independent asset budgets
        run: |
          ./target/release/phoenix-perf report \
            --capture target/perf/assets.json --gate > /dev/null

  # ── Demo balance regression report (issue #915, non-gating) ───────────────────
  #
  # Runs the standing destroyer-vs-each-combat_test-enemy matchup batch
  # (scripts/balance-runs.demo.toml) on fixed seeds and uploads the merged
  # summary plus every per-run AAR report as an artifact. Like `perf`, this job
  # REPORTS rather than gates: it is not in `deploy`'s `needs`, and the config's
  # thresholds are recorded in the output, never enforced — the job only goes
  # red on infrastructure failure (binary won't build, runner crashes). Gating
  # starts once the recorded numbers have proven stable across reports.
  balance:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust and cache
        uses: jkeywo/vellum/actions/setup-fleet-rust@582a61206d39c13d051d2e73bf463bb5d167d484

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libudev-dev libxkbcommon-dev libasound2-dev

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      # balance-runs.mjs imports smol-toml.
      - name: Install Node dependencies
        run: npm install

      # Release, matching the perf job and the recorded local baselines: the
      # batch fans out one process per (matchup x seed) and a debug binary
      # would make the job the slowest in the workflow.
      - name: Build headless runner
        run: cargo build --release --features headless --bin phoenix-headless

      - name: Run the demo matchup batch
        run: node scripts/balance-runs.mjs scripts/balance-runs.demo.toml --out target/balance

      - name: Publish summary
        if: always()
        run: |
          {
            echo '## Demo balance report (recorded thresholds, non-gating)'
            cat target/balance/summary.md 2>/dev/null || echo 'no summary produced'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload balance report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: balance-report
          path: target/balance/
          retention-days: 14

  # ── Deploy (main branch only, requires both test + smoke to pass) ─────────────
  deploy:
    needs: [test, build, editor-test, smoke]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
